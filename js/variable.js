const id=1234;//cannot re-declare and cannot re-assign
let name="sudheer";//cannot re-declare but we can re-assign //block scope
var balance=25000;//can re-declare and can re-assign //local scope
address="palasa";//global scope
console.table({id,name,balance,address});
